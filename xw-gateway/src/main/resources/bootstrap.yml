spring:
  cloud:
    nacos:
      discovery:
        namespace: hy-test
      server-addr: http://192.168.32.46:8848
      config:
        file-extension: yaml
        namespace: hy-test
  application:
    name: gateway

